;;; bears-beacon.el --- local styles
;;; Commentary:

;;; Code:

(use-package beacon
  :config
  (beacon-mode 1)
  (setq beacon-blink-when-window-scrolls nil)
  )

;; Local Variables:
;; byte-compile-warnings: (not free-vars)
;; End:

;;; bears-beacon.el ends here
